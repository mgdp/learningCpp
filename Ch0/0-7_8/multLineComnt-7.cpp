// a small c++ program
#include <iostream>

int main()
{
    /* this is a comment that extends ove rseveral lines 
       because it uses /\* and *\/ as starting and ending delimiters. */
    std::cout << "How about this, does this work?" << std::endl;
    return 0;
}
